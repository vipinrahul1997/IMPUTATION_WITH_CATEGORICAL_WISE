Categorical imputation is a technique used to handle missing values in categorical 
columns by replacing missing values with an appropriate category.

The most common method for categorical data is Mode Imputation.

Mode = the category that occurs most frequently.

Categorical imputation is the process of replacing missing values in categorical features with an appropriate category, 
commonly the mode or a new category such as "Unknown".
